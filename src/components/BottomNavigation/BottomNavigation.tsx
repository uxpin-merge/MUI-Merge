import React from 'react';
import PropTypes from 'prop-types';
import BottomNavigationM, { BottomNavigationProps } from '@mui/material/BottomNavigation';

/**
 * @uxpindocurl https://mui.com/api/bottom-navigation/
 */
function BottomNavigation(props: BottomNavigationProps) {
  return <BottomNavigationM {...props} />
}

export default BottomNavigation;
