import * as React from 'react';
import LoadingButton from '../LoadingButton';

export default (
  <LoadingButton loading variant="outlined" fullWidth={false} uxpId="loading-button-1">
    Loaded!
  </LoadingButton>
);
