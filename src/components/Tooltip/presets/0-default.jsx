import React from 'react';
import IconButton from '../../IconButton/IconButton';
import Tooltip from '../Tooltip';

export default (
  <Tooltip title="Title Here" uxpId="tooltip-0" PopperProps={{ keepMounted: true }}>
    <IconButton uxpId="Icon-Button-1">notifications</IconButton>
  </Tooltip>
);
