import * as React from 'react';
import Button from '../Button';
import Icon from '../../Icon/Icon';

export default (
  <Button uxpId="Button-1" color="primary" size="medium" variant="contained" startIcon={<Icon uxpId="button-icon">delete</Icon>}>
    Button
  </Button>
);
