import * as React from 'react';
import ListSubheader from '../ListSubheader';

export default (
  <ListSubheader uxpId="List-Subheader-1" disableSticky={true} color={'inherit'}>
    SubHeader Text
  </ListSubheader>
);
